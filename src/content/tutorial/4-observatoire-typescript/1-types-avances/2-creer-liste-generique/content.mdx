---
type: lesson
title: Créer une liste générique
slug: creer-liste-generique
template: observatory-02
focus: /src/App.tsx
custom:
  id: observatory-02
  world: 4
  order: 2
  prerequisites: [observatory-01]
  estimatedMinutes: 10
  xp: 110
  difficulty: avancé
  concepts:
    - génériques
    - render prop
  contentVersion: 1
  bonus: false
---

import ExerciseActions from '@/components/ExerciseActions';

# Créer une liste générique

Cette mission fait travailler **génériques** et **render prop** dans un petit composant exécutable.

## Objectif

Rendre des objets typés avec un composant `List<T>` et une render prop.

## Étapes

1. Ouvrez `src/App.tsx` et repérez le commentaire `TODO`.
2. Complétez la logique indiquée sans modifier le contrat public du composant.
3. Exécutez l'application, puis lancez la vérification comportementale.

## Résultat attendu

Rendre des objets typés avec un composant `List<T>` et une render prop. La vérification doit réussir après une interaction réelle avec l'interface lorsqu'elle est nécessaire.

<ExerciseActions
  client:load
  exerciseId="observatory-02"
  xp={110}
  contentVersion={1}
  hints={[
    'Commencez par le commentaire TODO : il cible uniquement la notion génériques.',
    'Rendre des objets typés avec un composant `List<T>` et une render prop.',
    'Comparez l’état ou le DOM avant et après l’action attendue, puis vérifiez les props et gestionnaires concernés.',
  ]}
/>
