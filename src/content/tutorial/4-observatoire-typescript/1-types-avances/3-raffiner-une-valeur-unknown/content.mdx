---
type: lesson
title: Raffiner une valeur unknown
slug: raffiner-une-valeur-unknown
template: observatory-03
focus: /src/App.tsx
custom:
  id: observatory-03
  world: 4
  order: 3
  prerequisites: [observatory-02]
  estimatedMinutes: 15
  xp: 110
  difficulty: avancé
  concepts:
    - unknown
    - type guard
  contentVersion: 1
  bonus: false
---

import ExerciseActions from '@/components/ExerciseActions';

# Raffiner une valeur unknown

Cette mission fait travailler **unknown** et **type guard** dans un petit composant exécutable.

## Objectif

Vérifier une réponse inconnue avant d’afficher son champ `name`.

## Étapes

1. Ouvrez `src/App.tsx` et repérez le commentaire `TODO`.
2. Complétez la logique indiquée sans modifier le contrat public du composant.
3. Exécutez l'application, puis lancez la vérification comportementale.

## Résultat attendu

Vérifier une réponse inconnue avant d’afficher son champ `name`. La vérification doit réussir après une interaction réelle avec l'interface lorsqu'elle est nécessaire.

<ExerciseActions
  client:load
  exerciseId="observatory-03"
  xp={110}
  contentVersion={1}
  hints={[
    'Commencez par le commentaire TODO : il cible uniquement la notion unknown.',
    'Vérifier une réponse inconnue avant d’afficher son champ `name`.',
    'Comparez l’état ou le DOM avant et après l’action attendue, puis vérifiez les props et gestionnaires concernés.',
  ]}
/>
