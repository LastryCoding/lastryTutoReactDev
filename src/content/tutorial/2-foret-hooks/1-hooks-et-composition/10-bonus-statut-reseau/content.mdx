---
type: lesson
title: 'Bonus : observer le statut réseau'
slug: bonus-statut-reseau
template: bonus-w2-02
focus: /src/App.tsx
custom:
  id: bonus-w2-02
  world: 2
  order: 10
  prerequisites: [forest-08]
  estimatedMinutes: 10
  xp: 60
  difficulty: intermédiaire
  concepts:
    - hook personnalisé
    - événements
  contentVersion: 1
  bonus: true
---

import ExerciseActions from '@/components/ExerciseActions';

# Bonus : observer le statut réseau

Cette mission fait travailler **hook personnalisé** et **événements** dans un petit composant exécutable.

## Objectif

Créer un hook qui réagit aux événements `online` et `offline`.

## Étapes

1. Ouvrez `src/App.tsx` et repérez le commentaire `TODO`.
2. Complétez la logique indiquée sans modifier le contrat public du composant.
3. Exécutez l'application, puis lancez la vérification comportementale.

## Résultat attendu

Créer un hook qui réagit aux événements `online` et `offline`. La vérification doit réussir après une interaction réelle avec l'interface lorsqu'elle est nécessaire.

<ExerciseActions
  client:load
  exerciseId="bonus-w2-02"
  xp={60}
  contentVersion={1}
  hints={[
    'Commencez par le commentaire TODO : il cible uniquement la notion hook personnalisé.',
    'Créer un hook qui réagit aux événements `online` et `offline`.',
    'Comparez l’état ou le DOM avant et après l’action attendue, puis vérifiez les props et gestionnaires concernés.',
  ]}
/>
