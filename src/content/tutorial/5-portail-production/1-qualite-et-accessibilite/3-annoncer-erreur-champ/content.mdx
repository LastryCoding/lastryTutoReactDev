---
type: lesson
title: Annoncer une erreur de champ
slug: annoncer-erreur-champ
template: portal-03
focus: /src/App.tsx
custom:
  id: portal-03
  world: 5
  order: 3
  prerequisites: [portal-02]
  estimatedMinutes: 20
  xp: 125
  difficulty: avancé
  concepts:
    - accessibilité
    - formulaire
  contentVersion: 1
  bonus: false
---

import ExerciseActions from '@/components/ExerciseActions';

# Annoncer une erreur de champ

Cette mission fait travailler **accessibilité** et **formulaire** dans un petit composant exécutable.

## Objectif

Associer un message d’erreur au champ avec `aria-describedby`.

## Étapes

1. Ouvrez `src/App.tsx` et repérez le commentaire `TODO`.
2. Complétez la logique indiquée sans modifier le contrat public du composant.
3. Exécutez l'application, puis lancez la vérification comportementale.

## Résultat attendu

Associer un message d’erreur au champ avec `aria-describedby`. La vérification doit réussir après une interaction réelle avec l'interface lorsqu'elle est nécessaire.

<ExerciseActions
  client:load
  exerciseId="portal-03"
  xp={125}
  contentVersion={1}
  hints={[
    'Commencez par le commentaire TODO : il cible uniquement la notion accessibilité.',
    'Associer un message d’erreur au champ avec `aria-describedby`.',
    'Comparez l’état ou le DOM avant et après l’action attendue, puis vérifiez les props et gestionnaires concernés.',
  ]}
/>
