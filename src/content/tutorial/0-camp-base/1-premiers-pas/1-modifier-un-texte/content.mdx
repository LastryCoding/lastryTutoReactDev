---
type: lesson
title: Modifier un premier composant
slug: modifier-un-texte
template: camp-01
focus: /src/App.tsx
custom:
  id: camp-01
  world: 0
  order: 1
  prerequisites: []
  estimatedMinutes: 10
  xp: 50
  difficulty: initiation
  concepts:
    - JSX
    - composant
  contentVersion: 1
  bonus: false
---

import ExerciseActions from '@/components/ExerciseActions';

# Votre premiere mission

Le composant affiche deja une carte de bienvenue. Modifiez son texte pour faire
apparaitre **Bienvenue dans React Quest !**, puis observez le resultat a droite.

## Objectif

Remplacer le texte du titre dans `src/App.tsx` sans changer la structure du
composant.

## Etapes

1. Ouvrez `src/App.tsx`.
2. Reperez le commentaire `TODO 1`.
3. Remplacez le texte du titre.
4. Enregistrez puis observez la previsualisation.

:::tip
Le texte visible entre `<h1>` et `</h1>` est du contenu JSX.
:::

## Resultat attendu

Une carte sombre portant le titre « Bienvenue dans React Quest ! » et un court
message de confirmation.

<ExerciseActions
  client:load
  exerciseId="camp-01"
  xp={50}
  contentVersion={1}
  hints={[
    'En JSX, le texte visible peut etre ecrit directement entre une balise ouvrante et sa balise fermante.',
    'La seule ligne a modifier est celle qui contient la balise h1 dans src/App.tsx.',
    'Le resultat doit prendre la forme : <h1>Bienvenue dans React Quest !</h1>.',
  ]}
/>
